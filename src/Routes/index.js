import { useRoutes } from 'react-router'

const MainRoutes = () => {
    let routes = useRoutes([
        {
            // path: '/',
            // element: <MainLayout />,
        },
    ])
    return routes
}